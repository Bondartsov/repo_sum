# 🧪 Аудит тестов (M2.5 финализация)

**Дата:** 24 сентября 2025
**Статус:** M2.5 - ФИНАЛИЗАЦИЯ (устранение блокеров VM backend и performance benchmarking)

---

## 1. Тесты, завязанные на старый `Embedder` или `mock_cpu_embedder`
- `tests/mocks/mock_cpu_embedder.py`  
  → Используется для подмены локальной CPU-модели.  
  🔧 **Рекомендация:** заменить на `MockRemoteEmbedder`, который будет имитировать сетевые вызовы (с задержками, ошибками).

- `tests/test_cpu_query_engine.py`  
- `tests/test_simple_cpu_query_engine.py`  
  → Явно используют CPU-локальный `Embedder`.  
  🔧 **Рекомендация:** переписать фикстуры на `RemoteEmbedder` или мок удалённого клиента.

- `tests/rag/test_rag_integration.py`  
- `tests/rag/test_rag_e2e_cli.py`  
- `tests/rag/test_query_engine_health.py`  
  → Проверяют интеграцию с RAG, вероятно, через локальный `Embedder`.  
  🔧 **Рекомендация:** адаптировать под `RemoteEmbedder`, добавить моки сетевых вызовов.

---

## 2. Тесты, не учитывающие сетевую специфику
- `tests/test_offline_no_network.py`  
  → Предполагает работу без сети, что противоречит новой архитектуре.  
  🔧 **Рекомендация:** либо удалить, либо переписать так, чтобы проверялось корректное падение при отсутствии сети.

- `tests/test_integration_full_cycle.py`  
  → Проверяет полный цикл, но без учёта сетевых ошибок.  
  🔧 **Рекомендация:** добавить сценарии с таймаутами, отказами соединения.

- `tests/rag/test_rag_performance.py`  
  → Меряет производительность локальной модели.  
  🔧 **Рекомендация:** переписать под сетевые задержки, добавить baseline для удалённой модели.

---

## 3. Тесты, которые уже частично покрывают сетевую специфику
- `tests/vm/test_vm_connectivity.py`  
- `tests/vm/test_vm_firewall_config.py`  
- `tests/vm/test_vm_health_monitoring.py`  
  → Эти тесты уже проверяют сетевую часть, их можно расширить для эмуляции ошибок `RemoteEmbedder`.

---

## ✅ Итоговые рекомендации
1. Создать новый мок `MockRemoteEmbedder` вместо `mock_cpu_embedder`.
2. Переписать CPU-тесты (`test_cpu_query_engine.py`, `test_simple_cpu_query_engine.py`) под удалённый интерфейс.
3. В интеграционных тестах (`test_rag_integration.py`, `test_rag_e2e_cli.py`, `test_integration_full_cycle.py`) добавить обработку сетевых ошибок (таймауты, отказ соединения).
4. В тестах производительности (`test_rag_performance.py`) учитывать сетевые задержки.
5. Тесты на оффлайн-режим (`test_offline_no_network.py`) пересмотреть: теперь они должны проверять корректное падение при отсутствии сети.

---

📌 Этот документ фиксирует текущее состояние проблемных тестов и шаги по их адаптации под новую архитектуру с `RemoteEmbedder`.
⚠️ Все тесты должны учитывать унифицированный стандарт 1024d и новый формат health check (`check_health()`).
