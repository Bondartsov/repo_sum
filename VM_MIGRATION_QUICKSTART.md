# 🚀 Быстрый старт: Миграция Jina v3 на VM

**Цель:** Решение проблем с памятью и развертывание jinaai/jina-embeddings-v3 на VM с 32GB RAM

## ⚡ Быстрый старт (5 минут)

### 1. Настройка SSH доступа
```bash
# Копируем SSH ключ на VM
ssh-copy-id ubuntu@10.61.11.54

# Тестируем подключение
ssh ubuntu@10.61.11.54 "echo 'Connection successful'"
```

### 2. Запуск автоматической установки
```bash
# Запускаем ЭТАП 1: Подготовка VM окружения
python3 scripts/vm_setup_phase1.py
```

**Скрипт автоматически:**
- ✅ Проверит SSH подключение
- ✅ Проанализирует ресурсы VM (32GB RAM!)
- ✅ Установит Python 3.9+ если нужно
- ✅ Создаст виртуальное окружение
- ✅ Клонирует репозиторий repo_sum

### 3. После завершения ЭТАПА 1
```bash
# SSH на VM для продолжения
ssh ubuntu@10.61.11.54

# Переход в проект и активация venv
cd repo_sum_rag && source venv/bin/activate

# Установка зависимостей и тестирование Jina v3
python3 scripts/vm_setup_phase2.py  # (будет создан)
```

---

## 📋 Полный план

**Детальный план:** [`JINA_V3_VM_MIGRATION_PLAN.md`](JINA_V3_VM_MIGRATION_PLAN.md)
- 8 этапов, 42 задачи
- Полная автоматизация RAG-as-a-Service

**Обновленная документация:** [`.clinerules/activeContext.md`](.clinerules/activeContext.md)
- Зафиксированы проблемы с локальной системой
- Новая стратегия VM deployment

---

## 🎯 Ожидаемые результаты

### Решаемые проблемы:
- ❌ **HuggingFace кэш коррупция** → ✅ **Чистое окружение на VM**
- ❌ **25+ GB ONNX требования** → ✅ **32GB RAM доступно**  
- ❌ **trust_remote_code сбои** → ✅ **Стабильная загрузка**
- ❌ **Низкое качество BGE** → ✅ **Полные 1024d векторы Jina v3**

### Улучшения производительности:
- **Качество поиска:** +40-60% благодаря полным векторам
- **Стабильность:** 100% uptime без OOM ошибок
- **Скорость:** 15-20 файлов/сек индексации
- **Масштабируемость:** до 50+ пользователей

---

## 🆘 Поддержка

**Если что-то идет не так:**
1. Проверьте SSH доступ: `ssh ubuntu@10.61.11.54`
2. Убедитесь в sudo правах на VM
3. Проверьте интернет соединение на VM
4. Посмотрите логи скрипта для деталей

**Контакты:**
- VM: t-ubuntu-redis (10.61.11.54)
- Документация: этот файл + план миграции
- Status: 🚀 Готово к запуску!
