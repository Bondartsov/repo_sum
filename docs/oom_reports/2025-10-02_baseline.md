# Baseline snapshot — 2025-10-02 incident

## Memory and swap
- Memory usage during failure: 62.68 GB / 62.79 GB (99.8%) — ref rules/!!!!ATTENTION(02_10_2025).md lines 29-33.
- Available RAM reported: ~100 MB.
- Swap status: active thrashing (100+ page faults / request). Effective swap size not recorded; collect via `free -h`.

## Runtime versions
- Python: 3.10 (venv path in vm_setup.log lines 43-48).
- Process manager: uvicorn 0.35.0 (requirements check in vm_setup.log line 59). Gunicorn not present.
- Embedding model: jina-ai/jina-embeddings-v3 (570M params, FP32) per rules/!!!!ATTENTION(02_10_2025).md lines 60-73.
- qdrant-client (SDK): 1.15.1 (vm_setup.log line 19).
- Qdrant server: docker image `qdrant/qdrant` (tag unspecified, start logic in vm_start.py lines 570-576). Confirm running version via `docker inspect`.

## Backup snapshot
- Executed `python scripts/backup_env_settings.py` -> backups/migration_backup_20251003_112326.
- Copied `config.py` -> `config.py.backup` and `vm_rag_service.py` -> `vm_rag_service.py.backup`.

## Next data to capture
- Output of `free -h`, `swapon --show`, `uvicorn --version`, `docker ps | grep qdrant`.
- Qdrant collection stats (vectors, disk usage).
